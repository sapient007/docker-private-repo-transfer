# docker-private-repo-transfer

### concourse pipeline that can be used to pull public docker images and port them to local docker repo. can also be used to sync private image repos in offline environments
